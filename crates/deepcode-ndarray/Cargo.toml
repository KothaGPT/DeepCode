[package]
authors = ["nathanielsimard <nathaniel.simard.42@gmail.com>"]
categories = ["science", "no-std", "embedded", "wasm"]
description = "Ndarray backend for the Deepcode framework"
documentation = "https://docs.rs/deepcode-ndarray"
edition.workspace = true
keywords = ["deep-learning", "machine-learning", "data"]
license.workspace = true
name = "deepcode-ndarray"
readme.workspace = true
repository = "https://github.com/kothagpt/deepcode/tree/main/crates/deepcode-ndarray"
version.workspace = true

[lints]
workspace = true

[features]
blas-accelerate = [
    "blas-src/accelerate", # Accelerate framework (macOS only)
    "ndarray/blas",
]
blas-netlib = ["blas-src/netlib", "ndarray/blas"]
blas-openblas = ["blas-src/openblas", "ndarray/blas", "openblas-src"]
blas-openblas-system = [
    "blas-src/openblas",
    "ndarray/blas",
    "openblas-src/system",
]
default = ["std", "simd", "multi-threads"]
doc = ["default"]
multi-threads = [
    "ndarray/rayon",
    "deepcode-common/rayon",
    "matrixmultiply/threading",
]
simd = ["macerator", "bytemuck", "seq-macro", "itertools"]
std = [
    "deepcode-autodiff",
    "deepcode-common/std",
    "deepcode-tensor/std",
    "deepcode-ir/std",
    "ndarray/std",
    "matrixmultiply/std",
    "rand/std",
    "rand/std_rng",
    "num-traits/std",
    "macerator/std",
]

# Serves as a ref impl for some deepcode-deepcl kernels
export_tests = []

[dependencies]

# ** Please make sure all dependencies support no_std when std is disabled **

deepcode-autodiff = { path = "../deepcode-autodiff", version = "0.1.0", default-features = false, optional = true }
deepcode-common = { path = "../deepcode-common", version = "0.1.0", default-features = false }
deepcode-ir = { path = "../deepcode-ir", version = "0.1.0", default-features = false }
deepcode-tensor = { path = "../deepcode-tensor", version = "0.1.0", default-features = false }

atomic_float = { workspace = true }
blas-src = { workspace = true, default-features = false, optional = true }      # no-std compatible
const-random = { workspace = true }
derive-new = { workspace = true }
libm = { workspace = true }
matrixmultiply = { workspace = true, default-features = false }
ndarray = { workspace = true }
num-traits = { workspace = true }
openblas-src = { workspace = true, optional = true }
paste = { workspace = true }
rand = { workspace = true, default-features = false, features = ["small_rng"] }
spin = { workspace = true }

# SIMD
bytemuck = { workspace = true, optional = true }
itertools = { version = "0.14", optional = true }
macerator = { workspace = true, optional = true }
seq-macro = { version = "0.3", optional = true }

[target.'cfg(not(target_has_atomic = "ptr"))'.dependencies]
portable-atomic-util = { workspace = true }

[dev-dependencies]
deepcode-autodiff = { path = "../deepcode-autodiff", version = "0.1.0", default-features = false, features = [
    "export_tests",
] }
deepcode-tensor = { path = "../deepcode-tensor", version = "0.1.0", default-features = false, features = [
    "export_tests",
] }

[package.metadata.docs.rs]
features = ["doc"]
rustdoc-args = ["--cfg", "docsrs"]
